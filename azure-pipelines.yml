resources:
  repositories:
  - repository: self
    type: git
    ref: main
jobs:
- job: bumpPatchVersion
  displayName: Auto bump Patch Version
  pool:
    vmImage: ubuntu-latest
  steps:
  - checkout: self
    displayName: Checkout repository
    persistCredentials: true

  - script: |-
        # Write your commands here
        echo 'Hello world'
        git config user.name "SrikanthPandanaboina"
        #git config user.email srikanth.pandanaboina39@gmail.com
        GIT_COMMITTER_DATE="$(git show --format=%aD | head -1)"
        git tag --annotate "1.1.3" --message 'My version '
        git tag --annotate "1.1.3+Serenity.version"--message 'My version meta tag'
        git push origin --tags

  # - task: GitVersion@5
  #   displayName: GitVersion
  - task: GitHubRelease@1
    displayName: GitHub release (create)
    inputs:
      gitHubConnection: d40ecbeb-33bd-451f-84d2-2c5ca69ffd37
...
